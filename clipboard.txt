

from github import Github
import pyperclip

# GitHub access token and repository information
ACCESS_TOKEN = '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'
REPO_NAME = 'ClipBoard_Sync'
REPO_OWNER = 'KarthikeyaKollu'

# Initialize PyGitHub client with access token
g = Github(ACCESS_TOKEN)

# Get the repository object
repo = g.get_user(REPO_OWNER).get_repo(REPO_NAME)

# Get the clipboard data
clipboard_data = pyperclip.paste()

# Overwrite the existing file with the new clipboard data
file_name = 'clipboard.txt'
commit_message = "Overwrite existing file with clipboard data"
repo.update_file(file_name, commit_message, clipboard_data)

print("Clipboard data overwritten in file in GitHub repository.")
